apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: alpha
type: Opaque
stringData:
  # NOTE: In production, use external secret management (e.g., Vault, External Secrets Operator)
  # This is a placeholder that should be overridden via CI/CD secrets
  database-url: "postgresql://user:password@postgres:5432/cicd_training_alpha?sslmode=disable"
