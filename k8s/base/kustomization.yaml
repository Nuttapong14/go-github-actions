apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: cicd-training-base

commonLabels:
  app.kubernetes.io/part-of: cicd-training
  app.kubernetes.io/managed-by: kustomize

resources:
  - backend
  - frontend
