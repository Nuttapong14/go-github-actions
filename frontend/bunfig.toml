# Bun configuration for CI/CD Training Frontend

[test]
# Test file patterns
root = "./tests"
preload = ["./tests/setup.ts"]

# Coverage settings
coverage = true
coverageDir = "./coverage"
coverageReporters = ["text", "lcov"]

# Test environment
timeout = 5000
